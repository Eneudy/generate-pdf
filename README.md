# Generate PDF

A python wrapper class that generates a ticket pdf, with a logo image.

## Install dependencies

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
python generate_pdf.py
```
